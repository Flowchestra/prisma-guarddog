/**
 * Config helpers for `prisma-guarddog`.
 *
 * Phase 1 surface (implementation pending):
 *   - `defineConfig()` — declares the consumer's `guarddog.config.ts`
 *
 * Resolution order (see ADR-0009):
 *   1. Explicit values in `guarddog.config.ts`
 *   2. Paths read from `prisma.config.ts`
 *   3. Convention (`./prisma/schema.prisma`, `./prisma/migrations/`)
 */

export {};
