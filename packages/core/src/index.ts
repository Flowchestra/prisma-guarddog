/**
 * `@prisma-guarddog/core` — policy compiler primitives.
 *
 * Phase 1 surface (implementation pending):
 *   - `defineClaims()` — declare the JWT claim shape
 *   - `defineDbRoles()` — declare Postgres roles
 *   - `defineBusinessRoles()` — declare application roles
 *   - `Guarddog` — constructor; `.resources()`, `.model()`, `.policy()`,
 *     `.columnPrivileges()`, `.polymorphic()`, `.noPolicy()`, `.rawSql()`,
 *     `.emit()`, `.diff()`, `.migrate()`
 *
 * See ../../../docs/PLAN.md and ../../../docs/adr/.
 */

export {};
