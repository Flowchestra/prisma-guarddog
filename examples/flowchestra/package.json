{
  "name": "example-flowchestra",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end example: 5 representative Flowchestra-shape policies authored with @flowchestra/prisma-guarddog-preset.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "type-check": "tsgo --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:e2e": "GUARDDOG_E2E=1 vitest run"
  },
  "dependencies": {
    "@flowchestra/prisma-guarddog-core": "workspace:*",
    "@flowchestra/prisma-guarddog-lint": "workspace:*",
    "@flowchestra/prisma-guarddog-preset": "workspace:*",
    "@flowchestra/prisma-guarddog-testing-postgres": "workspace:*",
    "@flowchestra/prisma-guarddog": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "pg": "^8.13.1",
    "@types/pg": "^8.11.10"
  }
}
